// UserInfo = this is the user information that is received from the user data server

export type UserInfo = {
    name: string,
    email: string,
    phone: string,
    img: string,
    isPatient: boolean
}